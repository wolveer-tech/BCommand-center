{
  "type": "module",
  "scripts": {
    "build": "node scripts/build-transfers.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check-project.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@mmmike/web-push": "^1.3.0",
    "aws4fetch": "^1.0.20",
    "hash-wasm": "^4.12.0",
    "wrangler": "^4.35.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "miniflare": "5.20260908.0-alpha"
  }
}
